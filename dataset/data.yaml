# 本项目下的dataset中的数据集仅供大家参考存放格式和调试代码是否能跑通
# dataset path
#path: /root/code/project/ultralytics/
#train: ./dataset/images/train  # path + train
#val: ./dataset/images/val # path + val
#test: ./dataset/images/test # path + test

# number of classes
#nc: 1

# class names
#names: ['ship']
# ====================== YOLO11 数据集配置文件 data.yaml 最终正确版 ======================
# 1. 训练集图片路径：必须是绝对路径，指向 images/train
train: D:\wenjian\person-pose\images\train
# 2. 验证集图片路径：必须是绝对路径，指向 images/val
val: D:\wenjian\person-pose\images\val

# 3. 只改这2处！！！其他内容不要动！！！
nc: 1        # 你的检测类别总数 → 比如检测1类写1，检测2类写2，以此类推
names: ['person']  # 你的类别名称 → 用英文，单引号包裹，多个类别用逗号分隔
                   # 示例：检测人+车 → names: ['person', 'car']
kpt_shape: [13, 3]
# =====================================================================================